{
  "name": "hostile-waters-modifier",
  "version": "1.0.3",
  "description": "Hostile Waters Modifier",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . hostile-waters-modifier --platform=win32 --arch=x64 --out=./out --app-version=1.0.1 --overwrite --asar"
  },
  "devDependencies": {
    "electron": "^9.0.5",
    "electron-packager": "^14.2.1"
  }
}
